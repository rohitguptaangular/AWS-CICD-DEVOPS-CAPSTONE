apiVersion: v1
kind: Service
metadata:
  name: herovire-app
  labels:
    app: herovire-app
spec:
  type: LoadBalancer
  selector:
    app: herovire-app
  ports:
    - name: http
      port: 80
      targetPort: 5000
      protocol: TCP
