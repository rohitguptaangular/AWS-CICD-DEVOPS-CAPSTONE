# Inventory = the servers Ansible configures, and how to reach them.
#
# Fill in the real public IP after `terraform apply` (see the jenkins_public_ip
# output). Connection details (user + key) come from ansible.cfg.
#
# NOTE: the IP below is a PLACEHOLDER — replace it before running for real.

[jenkins]
JENKINS_PUBLIC_IP

# Later (Sprint 4+) we can add the EKS worker nodes as another group:
# [eks_nodes]
# 10.0.11.x
# 10.0.12.x
